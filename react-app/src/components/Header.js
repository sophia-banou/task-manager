import '../css/Header.css'

const Header = () => {
    return (
        <header class="header">
            <img src="logo.png" alt="Logo" class="logo" />
        </header>
    );
};

export default Header;